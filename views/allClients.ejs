<%- include("partials/header") %>
    <div class="container bg-gradient">
        <div class="row text-center">
            <div class="col-md-4">

            </div>
            <div class="col-md-4">
                <a href="/"><button class="btn btn-success">Начална страница</button></a>
            </div>
            <div class="col-md-4">

            </div>
        </div>
        <div class="row text-center">
            <table class="table table-secondary bg-gradient table-reponsive">
                <thead>
                    <tr>
                        <th scope="col">Дата</th>
                        <th scope="col">Име</th>
                        <th scope="col">Телефон</th>
                        <th scope="col">Информация</th>
                        <th scope="col">Сума</th>
                        <th scope="col">Предаден</th>
                    </tr>
                </thead>
                <tbody>
                    <% clients.map((client)=> { %>
                        <tr class="<%= client.fixed === true ? '' : '' %>">
                            <td class="<%= client.fixed  ? 'bg-success' : '' %>">
                                <%= client.date %>
                            </td>
                            <td class="<%= client.fixed  ? 'bg-success' : '' %>">

                                <%= client.name %>
                            </td>
                            <td class="<%= client.fixed  ? 'bg-success' : '' %>">
                                <%= client.phone %>
                            </td>
                            <td class="<%= client.fixed  ? 'bg-success' : '' %>">
                                <%= client.information %>
                            </td>
                            <td class="<%= client.fixed  ? 'bg-success' : '' %>">
                                <%= client.money %>
                            </td>
                            <td class="<%= client.fixed  ? 'bg-success' : '' %>">
                                <form action="/clients/<%= client.id %>" method="Patch">
                                    <button type="submit"
                                        class="<%= client.fixed === true ? 'btn btn-secondary' : 'btn btn-success' %>">
                                        <%= client.fixed===true ? 'Върни' : 'Завърши' %>
                                    </button>

                                </form>
                            </td>
                        </tr>
                        <% }); %>
                </tbody>
            </table>

        </div>
    </div>

    <%- include("partials/footer") %>